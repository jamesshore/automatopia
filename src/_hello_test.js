// Copyright Titanium I.T. LLC.
"use strict";

// A real codebase would use a npm module for the test suite here.
const { suite, assert } = require("../_build/node_modules/tests");

module.exports = suite(({ it }) => {

	it("runs tests", () => {
		// assert.todo();
	});

});