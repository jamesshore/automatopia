import { beforeAll } from "ergotest";


// This is an optional file that you can use to define global before/after functions.
// Delete it if not needed.

beforeAll(() => {
	// global "before all" code goes here
});
